# Bubblewrap build output + secrets — never commit the signing key.
android.keystore
*.apk
*.aab
build/
node_modules/
# Generated by `bubblewrap init`; commit twa-manifest.json once created if you
# want reproducible rebuilds (it contains no secrets).
